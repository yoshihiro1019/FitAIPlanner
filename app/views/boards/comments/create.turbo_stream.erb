<%= turbo_stream.append "table-comment", partial: "boards/comments/comment", locals: { comment: @comment } %>
<%= turbo_stream.replace "new_comment_form", partial: "boards/comments/form", locals: { board: @board, comment: Comment.new } %>
